// defines application level constants
//    donot change

module.exports = {

    'paths' : {						// constants defining the application paths
        'routes'        	: __dirname + '/routers',
        'views'    				: __dirname + '/views',
        'models'     			: __dirname + '/models',
        'app'							: __dirname + '/app',
        'api'							: __dirname + '/api'
    },

};
